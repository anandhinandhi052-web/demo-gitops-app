pipeline {
  agent any

  stages {
    stage('Checkout') {
      steps {
        echo "Code checked out from GitHub"
      }
    }

    stage('CI Validation') {
      steps {
        echo "CI checks passed (lint/tests placeholder)"
      }
    }
  }
}
